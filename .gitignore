# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Debug/Error output files
*_output.txt
*_errors*.txt
compile_*.txt
tsc_*.txt

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code
.claude/*
!.claude/rules/
!.claude/commands/
!.claude/skills/
!.claude/memory/
!.claude/agents/
!.claude/agent-memory/
!.claude/hooks/

# Project secrets
SUPABASE_INFO.md
AUDIT_FUNCIONAL.md
.env.production

# Test exports and debug scripts
exports/test-*
*.bak
/*.mjs
!eslint.config.mjs
scripts/one-shot/

# Supabase backups (local snapshots from scripts/_backup.mjs)
backups/

# Archived legacy modules (kept on disk, not in git)
archive/

# One-shot audit reports
AUDIT_*.json
AUDIT_*.txt
audit_summary.csv
type_errors.txt
phantom_x_report.txt
stress_report.txt
REPORTE_NOCHE.md

# Test coverage & results
coverage/
test-results/


# Auto-generated plans and audit reports from scripts
tmp/

# Inventarios/planes de la gestion de AMFEs (datos internos: clientes, rutas de servidor, dumps)
reports/
exports/oficializar/

# OneDrive junction with sensitive Patagonia docs (NEVER commit)
docs-local/

# ARB ERP export cache (datos de negocio: part numbers, consumos, proveedores — NEVER commit; repo es publico)
.arb-cache/

# ESLint scratch logs
*eslint*.txt

# Scratch scripts (root-level standalone analysis)
/analyze.js

# Entorno CAD 3.12 (build123d/OCP) - NO commitear (pesa ~500MB)
.venv-cad/

# Python bytecode (cadlib del skill cad-design)
__pycache__/

# Cache local de documentos de empresa (repo publico — NUNCA commitear)
.sgc-cache/

# Extractos de documentos de la empresa (alertas, 8D, specs de cliente, SGC, IATF).
# Estuvieron versionados por error hasta 2026-07-29 (828 archivos, 259 MB, publicos).
# Ahora viven en .sgc-cache/empresa-extracted/. NO volver a commitear esta ruta.
docs/empresa-extracted/

# Datos de la empresa — NO commitear (repo publico)
REGISTRO_TIEMPOS_INYECCION.xlsx
